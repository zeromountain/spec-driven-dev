# ADR 양식

> 이 파일은 양식이다(이름이 `_`로 시작하는 파일은 에이전트 컨텍스트에서 빠진다).
> 결정을 기록할 때 이 파일을 복사해 `NNNN-<제목>.md`로 저장한다. 에이전트는 결정의
> **이유**를 알아야 그 결정을 뒤집는 구현을 피한다.

# ADR-NNNN: 제목

- 상태: 제안됨 | 채택됨 | 대체됨(ADR-XXXX)
- 날짜: YYYY-MM-DD

## 맥락

무엇 때문에 이 결정이 필요했는가.

## 결정

무엇을 하기로 했는가.

## 결과

이 결정으로 쉬워지는 것과 어려워지는 것. 검토했지만 버린 대안.
